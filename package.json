{
  "name": "codeintel",
  "private": true,
  "version": "0.1.0",
  "description": "CodeIntel monorepo — AI test engineer that lives in your repo.",
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "dev:dashboard": "pnpm --filter @codeintel/dashboard dev",
    "dev:vscode": "pnpm --filter @codeintel/vscode-extension watch"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
