{
  "name": "@codeintel/sdk",
  "version": "0.1.0",
  "description": "Typed client for the CodeIntel API. Shared by the dashboard and the IDE extensions.",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "lint": "tsc -p . --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
